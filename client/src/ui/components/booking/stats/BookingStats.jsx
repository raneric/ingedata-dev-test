import StatsCard from "./StatsCard";

/**
 * BookingStats component renders a card displaying the fulfillment rates for
 * rooms.
 *
 * It wraps the StatsCard component.
 *
 * @returns {JSX.Element} A JSX element containing the StatsCard component.
 */
function BookingStats() {
  return (
    <>
      <StatsCard />
    </>
  )
}

export default BookingStats;
