apiVersion: v1
kind: Service
metadata:
  name: { { .Values.backend.serviceName } }
  namespace: { { .Values.namespace } }
spec:
  selector:
    app: { { .Values.backend.appLabel } }
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
