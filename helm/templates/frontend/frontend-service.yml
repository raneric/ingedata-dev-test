apiVersion: v1
kind: Service
metadata:
  name: { { .Values.frontend.serviceName } }
  namespace: { { .Values.namespace } }
spec:
  type: LoadBalancer
  selector:
    app: { { .Values.frontend.appLabel } }
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
